// Package store is part of aiclibridge.
//
// It hosts persistence: session history, credential references, and
// the small SQLite-backed store. The schema and migrations land in
// a later milestone.
package store
